// QuantaLang Negative Test Suite
//
// These tests verify that the compiler REJECTS invalid programs.
// Each test case should produce an error and exit with non-zero.
// If the compiler accepts any of these, it's a bug.
//
// CURRENT STATUS:
//   PASSES (correctly rejected):
//     - Type mismatch: let x: i32 = "hello"
//     - Wrong arg count: add(1) when add takes 2 args
//     - Return type mismatch: fn() -> i32 { "string" }
//     - Undefined variable in expression: let y = x + 1
//     - Undefined variable as function arg: add(x, 1)
//     - Undefined function: foo()
//
//   KNOWN GAPS (incorrectly accepted):
//     - Undefined variable in println! macro args
//     - Syntax errors that parser recovers from (fn main( { })
//
// This file does NOT compile — it's documentation of the negative test results.
// The actual tests are run via shell commands in CI or manually.

fn main() {}
