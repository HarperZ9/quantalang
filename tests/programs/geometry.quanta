pub struct Point {
    pub x: f64,
    pub y: f64,
}

pub fn make_point(x: f64, y: f64) -> Point {
    Point { x, y }
}

pub fn distance(a: &Point, b: &Point) -> f64 {
    let dx = a.x - b.x;
    let dy = a.y - b.y;
    sqrt(dx * dx + dy * dy)
}
