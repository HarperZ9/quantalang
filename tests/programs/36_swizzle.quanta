fn main() {
    let color = vec4(1.0, 0.5, 0.2, 1.0);
    let rgb = color.xyz;
    println!("rgb: ({}, {}, {})", rgb.x, rgb.y, rgb.z);

    let rg = color.xy;
    println!("rg: ({}, {})", rg.x, rg.y);

    let flip = color.zyx;
    println!("flip: ({}, {}, {})", flip.x, flip.y, flip.z);
}
