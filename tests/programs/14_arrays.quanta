fn sum_array(arr: [i32; 5]) -> i32 {
    let mut total: i32 = 0;
    let mut i: i32 = 0;
    while i < 5 {
        total = total + arr[i];
        i = i + 1;
    }
    total
}

fn main() {
    let numbers = [10, 20, 30, 40, 50];
    let total = sum_array(numbers);
    println!("Sum: {}", total);
}
