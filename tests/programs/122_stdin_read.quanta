fn main() {
    if stdin_is_pipe() {
        let input = read_all();
        let trimmed = input.trim();
        let line_count = trimmed.len();
        println!("Read {} bytes from stdin", line_count);
    } else {
        println("No piped input detected");
    }
}
