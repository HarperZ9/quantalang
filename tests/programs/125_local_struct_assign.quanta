struct Point {
    x: i64,
    y: i64,
}

fn main() {
    let mut p = Point { x: 1, y: 2 };

    p.x = 10;
    p.y = 20;

    println!("x={}", p.x);
    println!("y={}", p.y);

    p.x = p.x + p.y;
    println!("sum={}", p.x);
}
